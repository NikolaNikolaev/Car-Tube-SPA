import inputFieldsData from './inputFieldsData';
import signSectionData from './signSectionData';

const formData = {
    id: 'login',
    header: 'Login',
    data: inputFieldsData,
    button: 'Register',
    signSectionData,
};

export default formData;