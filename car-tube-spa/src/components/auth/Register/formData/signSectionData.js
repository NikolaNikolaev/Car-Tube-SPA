import authEndpoints from '../../../../services/api/authEndpoints';

const signSectionData = {
    className: 'container signin',
    text: 'Already have an account?',
    endpoint: authEndpoints.LOGIN_FORM_ENDPOINT,
    endpointName: 'Sign In'
};

export default signSectionData;